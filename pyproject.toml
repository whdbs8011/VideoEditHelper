[project]
name = "premiere-storyboard-builder"
version = "1.3.1"
description = "Build Adobe Premiere Pro timelines from CSV or XLSX storyboards"
requires-python = ">=3.10"
dependencies = ["pymiere==1.4.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
